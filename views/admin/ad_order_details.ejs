<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/order_detail.css">
    <title><%= title %></title>
</head>
<body>
    <div class="container">
        <h1>Chi tiết đơn hàng</h1>
        <h3>Tên khách hàng: <%= order.userId.name %></h3>
        <h3>Địa chỉ: <%= order.address %></h3>

        <h3>Danh sách sản phẩm:</h3>
        <table class="order-table">
            <thead>
                <tr>
                    <th>Tên món</th>
                    <th>Số lượng</th>
                    <th>Giá</th>
                </tr>
            </thead>
            <tbody>
                <% order.items.forEach(item => { %>
                    <tr>
                        <td><%= item.itemName %></td>
                        <td><%= item.itemQty %></td>
                        <td><%= item.itemPrice.toFixed(2) %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>

        <h3>Hóa đơn:</h3>
        
        <p>Thành tiền: <%= order.bill.subTotal.toFixed(2) %></p>
        <p>Thuế: <%= order.bill.tax.toFixed(2) %></p>
        <p>Phí giao hàng: <%= order.bill.deliveryCharge.toFixed(2) %></p>
        <p>Tổng cộng: <%= order.bill.grandTotal.toFixed(2) %></p>

        <a href="/admin/order" class="back-link">Quay lại danh sách đơn hàng</a>
    </div>
</body> 
</html>
